import { BearerHeaderHttpInterceptor } from './bearer-header-http-interceptor';

describe('BearerHeaderHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new BearerHeaderHttpInterceptor()).toBeTruthy();
  });
});
